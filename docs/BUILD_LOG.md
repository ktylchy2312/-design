# Build Log

Chronological record of steps taken while building this environment. Append an entry whenever a step is completed — this is a narrative plan/history, not a save point (see [SAVE_POINTS.md](./SAVE_POINTS.md) for those).

## 2026-08-14

1. **Scaffolded the plugin.** Created `.claude-plugin/plugin.json` (name `claude-design`) and `agents/` as the home for subagent definitions.
2. **Created the orchestrator agent** (`agents/orchestrator.md`). Its job: for each task, decide directly vs. delegate vs. parallelize, optimizing for least total tokens and wall-clock time. Holds a registry table of known agents so routing is a lookup, not a re-derivation — currently empty, orchestrator is the only agent.
3. **Wrote `CLAUDE.md`.** Curated 4 principles from `assets/andrej-karpathy-skills-main` (Simplicity first, Surgical changes, State assumptions don't guess, Goal-driven), adapted to this project rather than copied verbatim. Rest of the structure follows Anthropic's documented CLAUDE.md conventions (headers/bullets, <200 lines, concrete/verifiable rules).
4. **Moved `andrej-karpathy-skills-main` into `assets/`.** It's vendored reference material, not part of the plugin — kept separate so it's clear it's not something this project edits or ships.
5. **Ran `git init`.** No commit yet — orchestrator and CLAUDE.md are still being iterated on. First commit will double as the first save point (see SAVE_POINTS.md).
6. **Widened orchestrator's scope.** It now coordinates three kinds of capability, not just agents: subagents, skills, and other installed plugins. Split its single registry table into three (Agents / Skills / Plugins) and added "invoke a skill" as a cheaper middle option between doing work directly and delegating to a subagent. `CLAUDE.md` updated to match.
7. **Added Anthropic's skill design principles to the orchestrator.** New "Skill principles" section: progressive disclosure (registry holds one-line descriptions only — full instructions and reference files load in later levels, only on actual use), composability (skills combine with agents/direct action, never treated as exclusive), portability (skills assumed surface-agnostic; a missing dependency is an environment gap, not grounds to avoid the skill).
8. **Added a code-writing section to `CLAUDE.md`.** Governs any code/script/bash this environment produces: senior full-stack engineer persona, simple over clever, break work into small verified steps instead of one large change, token economy as the governing constraint, no speculative code.
9. **Built the live Figma bridge, Phase 1 (read).** New `figma-plugin/` — a Figma Plugin API plugin (`code.js`/`ui.html`/`manifest.json`) plus a local relay (`bridge/server.mjs`) and one-shot CLI (`bridge/cli.mjs`) so Claude Code can drive it over Bash. Chosen over browser automation (user's call) and over an MCP-wrapped relay (unverified plugin-MCP auto-start behavior — CLI-shellout was the simpler, already-understood path). Reads live selection/page/node-tree — the thing the separately-installed `figma-bridge` plugin's static REST reads structurally can't see. Node.js wasn't installed on this machine; installed it (winget, user's choice) before `npm install`. Verified live: relay + plugin connect, `get_status`/`get_selection` return real data from the open document.
10. **Built Phase 2 (generic write) and the `figma-live-bridge` agent.** Re-scoped from the original plan's per-shape ops to one generic primitive: `create_node` (type: RECTANGLE/ELLIPSE/LINE/TEXT/FRAME), `set_node_property` (allowlisted props, handles async font-loading for text), `move_node`. Deliberately not building per-type or higher-level "how to build a button" ops here — that's pushed to a planned `design-system` skill/agent (Phase 3, not yet built) so `code.js` stays small. Added `agents/figma-live-bridge.md` and its row in `agents/orchestrator.md`'s Agents table.
11. **Verified Phase 2 live and fixed a real quoting bug.** Created a rectangle, a text node (content set via the async font-load path), and an auto-layout frame, then reparented the rectangle into it — geometry snapped to the frame's padding as expected, confirming auto-layout genuinely engaged. Along the way: PowerShell 5.1 silently mangles double-quoted JSON args containing spaces before they reach `node.exe` (confirmed via `process.argv` — quotes vanish, string splits on spaces). Fixed by adding an `@path/to/file.json` form to `cli.mjs` that reads params from a file instead of argv, sidestepping shell quoting entirely.
12. **Added batch mode to `cli.mjs` after a token-cost review.** The Phase 2 verification above took ~20 separate Bash/PowerShell calls for 6 logical Figma edits — one full tool round-trip per op, each with its own `$env:Path`/`Set-Location` overhead, on top of ~70k tokens already spent across the session's research/planning subagents. `cli.mjs --batch '@ops.json'` now runs a whole array of ops over one WebSocket connection in one process, printing one JSON-array result — verified live with a 3-op batch (status + create + selection) in a single call. `agents/figma-live-bridge.md` and `figma-plugin/README.md` updated to make batching the default for any multi-step task; `CLAUDE.md`'s code-writing section gained a general rule: batch a known sequence of shell calls into one script instead of one round-trip per step.
13. **Added `eval_script` to `code.js`** — `new Function('figma', ...)` wrapping arbitrary JS, `return` as the only output channel. This is Phase 3's core primitive for components/variants/foundations/typography variables; Phase 2's ops are kept as-is alongside it. Modeled on a mature reference (`figma-use`, from the official `figma/mcp-server-guide` repo) rather than hand-rolling typed ops per capability — see the plan file for the full reasoning. Not yet live-verified (pending plugin reload in Figma).
14. **Registered Figma's official remote MCP server, narrowly.** While reviewing `figma-create-new-file` (another skill from the same reference repo), confirmed the Plugin API structurally cannot create a brand-new Figma file — only Figma's remote MCP server can. Added `.mcp.json` (server `figma-official`, `https://mcp.figma.com/mcp`) plus `.claude/settings.json` explicitly allowing only `whoami`/`create_new_file` and denying the other 16 tools that server exposes (`use_figma`, `get_design_context`, etc.) by name — a real technical restriction, not just a documented convention, so the local bridge stays the only path for actual design read/write work. `agents/orchestrator.md`'s Plugins table gets its first row.
15. **Added visual/UI design principles to `CLAUDE.md`**, condensed from a pasted `frontend-design` reference skill (brief-specific choices over templated AI-design defaults, ground every design in its actual subject, two-pass brainstorm-then-critique-then-build, spend boldness in one signature element, copy as design material). Applies to the upcoming `design-system` agent's Figma work and any future generated HTML/CSS alike — no tool dependency, so integration was direct per the evaluation process (step 3 "verify" was trivially satisfied, nothing to fail).
16. **Session-level process audit.** Biggest single cost across the session so far: research/planning subagent dispatches (~122k tokens across 4 logged calls, likely ~150k+ including one unlogged Plan-agent run) — one of them (~52k tokens researching Figma's Variables/component API) turned out largely redundant with a reference skill already cached locally, found only after the fact. Added a "Process economy" section to `CLAUDE.md`: check local reference material before dispatching research subagents, match plan-mode ceremony to task size, fix recurring per-call environment costs (like a missing `PATH` entry) once via a session restart rather than a workaround on every call.
17. **Scaffolded `skills/figma-design-system/` and `agents/design-system.md`.** Skill kept structurally separate from `figma-plugin/` on purpose — the plugin only knows the generic `eval_script`/`create_node`/etc. primitive, the skill holds all component/variant/style/variable domain knowledge (condensed from the earlier-discovered reference skill: variant naming, `combineAsVariants` gotchas, style application, variable scopes/mode limits). `design-system` agent routes here, shells out to the same `cli.mjs` transport `figma-live-bridge` uses. Both registered in `agents/orchestrator.md`. Not yet live-verified — still blocked on the `eval_script` smoke test.
18. **Formalized "self-improving agents and skills" in `CLAUDE.md`.** After any agent/skill run surfaces a real gap (an error worked around, a rule that would've prevented a wasted step), fold it back into that agent's/skill's own file, not just narrated in this log — codifies what was already happening ad hoc (`cli.mjs`'s `@file` fix, the batch-mode addition, the design-system reference gotchas) as standing practice.
19. **Verified `eval_script` live and built a real component set.** Smoke test (`return figma.currentPage.name`) confirmed `new Function('figma', ...)` genuinely executes inside the Figma Plugin API sandbox — the single biggest unverified assumption in Phase 3. Then built a real "Button" component set with 4 state variants (Default/Hover/Pressed/Disabled) via one `eval_script` call — `combineAsVariants` correctly inferred the `State` variant property from the `"State=X"` naming convention (confirmed by reading `componentPropertyDefinitions` back). Phase 3's core primitive and the `figma-design-system` skill's central recipe are now proven, not just planned.
20. **Added `skills/ui-reference-analysis/`.** A pure-reasoning skill (no code, no bridge dependency) that turns screenshots dropped in `assets/screenshots/ui components/` into structured build briefs (layout/colors/typography/grouped variants) written to `<name>.brief.md` next to the source image — caches the analysis so a rerun skips already-briefed images. Deliberately stops at the brief; building it is a separate, explicit step for `design-system`. Registered in `agents/orchestrator.md`'s Skills table.
21. **Added the "page presentation" convention to `figma-design-system`.** Prompted by a user-supplied reference screenshot (color-palette documentation frames: dark title header over labeled swatch sections) — adapted into a reusable structural pattern (`references/page-presentation.md`), not copied. Verified live by wrapping the existing Button component set in a titled page frame in one `eval_script` call (`157:341`, 668×222) — dark header with title/subtitle, light content area, correct on the first try. Applies to both component and foundations output going forward.
22. **Built and verified foundations: spacing/radius variables + typography text styles.** Reference values pulled from `assets/screenshots/ui components/OC-Kontrol-Board-prototype.html`'s inline styles (grepped padding/gap/border-radius/font-size out of a 550KB bundled file) and consolidated into a clean scale rather than reproducing every ad-hoc pixel value — `Spacing` collection (8 vars, xs-4xl), `Radius` collection (4 vars, sm-xl), 6 `Type/*` text styles. Bound live to the button component (`cornerRadius`→`Radius/md`, padding→`Space/xl`/`Space/sm`, label→`Type/Label`) — first real end-to-end proof of variable + text-style binding working, not just creation. Mid-task the button's underlying nodes disappeared from the canvas (user made unrelated changes on the same page) — rebuilt in one consolidated `eval_script` call (create + bind + page-wrap together) rather than multiple round-trips; user explicitly asked for fewer diagnostic checks when live state looks off, noted in memory.
23. **Rebuilt the full token system and added three style-guide pages.** Superseded entry 22's smaller token set on request: grepped the actual hex colors and `font-family`/`font-weight` out of the same HTML reference (previous entry had wrongly assumed Inter — the file actually uses **Roboto**, confirmed live via `listAvailableFontsAsync`, which also revealed Roboto's style strings have no space, `"Bold"` not `"Semi Bold"`). Built: `Color` collection (31 vars — Neutral/Primary/Secondary/Success/Danger/Warning/Info), `Spacing` and `Radius` recreated in Tailwind's own numeric naming convention (`space-0`…`space-64`, `radius-none`…`radius-full` — deleted and replaced the old xs/sm/md set), `Breakpoints` collection (5 vars, `scopes: []`, reference-only). Hit a real bug: `createVariable` throws `invalid variable name` on a literal `.` in the name (`space-0.5`) — since script execution isn't transactional, had to clean up the partially-created collection before retrying; documented in the skill (`SKILL.md` rule 14, new `references/tokens-built.md` as ground truth for what now exists). Built three style-guide page frames (Colors, Typography, Layout tokens) using the `page-presentation.md` convention, each populated by looping over the real collections rather than hand-listing values.
24. **Renamed Spacing semantically and trimmed the scale.** Dropped the 160/192/256px steps (nothing in this file needs spacing past 128px) and replaced the numeric Tailwind names with usage-based ones (`space-card-padding`, `space-section-gap`, `space-icon-gap`, etc. — 16 tokens total), each carrying its pixel value in the variable's `description` field. Usage guidance recorded in `references/tokens-built.md`. Rebuilt the Layout tokens style-guide page to match.
25. **Fixed page frames all stacking at (0,0).** Every page-frame script (Colors/Typography/Layout tokens/Button) had violated the skill's own documented gotcha #13 (position new top-level nodes away from existing content) — none of them ever set an explicit `x`, so all landed on top of each other at the origin. Repositioned the three surviving frames into a non-overlapping row. Fixed `page-presentation.md`'s script pattern to compute an offset from existing top-level frames before placing a new one, so this doesn't recur.
26. **Ported all 28 OC-DS components into Figma.** Same inventory as `OC-DS`'s React build (10 primitives, 12 molecules, 6 organisms), built via `eval_script` batched 3-4 components per call (applying the just-learned batching lesson) — roughly 10 calls total instead of 28+. Every component bound to the real `Color`/`Spacing`/`Radius` variables and `Type/*` text styles. Hit and fixed two new gotchas along the way: new text nodes default to Inter Regular even when Roboto is loaded (must set `fontName` before `characters`, every time — `SKILL.md` rule 4a), and `figma.createVariable` rejects a literal `.` in names (already known, re-triggered cleanup discipline). Confirmed `component.createInstance()` / `node.clone()` for composing already-built pieces into new ones (`StageCard` uses a `Badge` instance, `AppShell` clones `Sidebar`+`KanbanBoard`), and `layoutWrap = 'WRAP'` for form-field grids. The Button component was found deleted again mid-task (user's own canvas edits) and rebuilt a third time. Final pass repositioned all 30 top-level nodes into one non-overlapping row.
